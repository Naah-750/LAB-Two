What are the values of the following expressions? In each line, assume that
        String s = "Hello";
        String t = "World";
        ---------------------------------------
// the length of the first word + the length of the other word = 10
        a. s.length() + t.length()
        ----------------------------------------
// it will show He word
        b. s.substring(1, 2)
        --------------------------------------
// it will show llo word
        c. s.substring(s.length() / 2, s.length())
        -----------------------------------------
// it will show  Helloworld
        d. s + t
        -----------------------------------------
// it will show WorldHello
        e. t + s
// thank u ☻